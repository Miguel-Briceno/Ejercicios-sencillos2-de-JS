    // funcion que se ejecuta cuando se produce el llamado a la funcion en el DOM
    // funcion cambiar a rojo se seleciona el elemento html de tipo caja de texto 
    // y se le cambia el color a rojo cuando se llama a la funcion
    function cambiarRojo(){document.getElementById('caja').style.backgroundColor="red"};
    // funcion cambiar a rojo se seleciona el elemento html de tipo caja de texto 
    // y se le cambia el color a azul cuando se llama a la funcion
    function cambiarAzul(){document.getElementById('caja').style.backgroundColor="blue"};// funcion cambiar a azul
    
